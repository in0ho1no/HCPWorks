export interface HeaderDisplayOptions {
  showName: boolean;
  showScope: boolean;
  showKind: boolean;
}

export const DEFAULT_HEADER_DISPLAY_OPTIONS: HeaderDisplayOptions = {
  showName: true,
  showScope: true,
  showKind: true,
};
